On the site EasyBuild host (herdr): follow @@SKILL_PATH@@ section 7 full-drive.
You OWN the campaign through REAL installs (*builds*).

Primary mechanical path (already rendered for this campaign):

  bash @@FULL_DRIVE_SCRIPT@@

That script: optional oracle copies, format-style/check-contrib/check-recipe
(--recipe), eb -Dr, then eb --robot per recipe with logs under WORK/logs/,
and writes WORK/residuals/session-log.md.

**Arch Linux host lessons (do not thrash pacman or invent packages):**
EasyBuild checks Debian/RHEL package *names* (`libibverbs-dev`,
`rdma-core-devel`). On Arch the package is `rdma-core` (provides libibverbs +
headers). If `/usr/include/infiniband/verbs.h` exists, the rendered full-drive
script already passes `--ignore-osdeps`. If headers are missing:
`sudo pacman -S --needed rdma-core` then re-run the script. Do **not** search for `libibverbs-dev` on Arch.
Do **not** reinstall `linux-api-headers` for `linux/scc.h` — kernel 7.x **removed** that UAPI
header; GCC 15 libsanitizer still includes it. Fix: GCCcore overlay
`--disable-libsanitizer` under `overlays/arch/` (auto-rsynced on Arch).
Do **not** disable sanity checks.

WORK=@@WORK@@
REPO=@@REPO@@
ROBOT=@@ROBOT@@
Recipes:
@@RECIPES_BULLETS@@

If a build fails for a real compile/link/sanity reason: read robot-*.log, fix
recipe/companion with justified edits (oracles/docs only — do not invent -D),
re-run gates + robot for that package, update session-log.md.

Print DONE_FULL_DRIVE or DONE_PARTIAL.

FORBIDDEN: edit REPO/src; open GitHub/GitLab PRs; invent product flags; disable sanity.

Start by running the full-drive script now.
