#+TITLE: ~eb-stack~
#+AUTHOR: [[https://rgoswami.me][Rohit Goswami]]
#+OPTIONS: toc:nil todo:nil num:nil author:nil title:nil ^:nil

#+begin_export rst
.. raw:: html

   <div class="eb-hero">
     <div class="eb-hero-brand">
       <img class="eb-hero-mark" src="_static/mark.svg" width="56" height="56" alt="" />
       <div>
         <p class="eb-hero-name">eb-stack</p>
         <p class="eb-hero-sub">package plan · Resolvo · EasyBuild campaign</p>
       </div>
     </div>
     <p class="eb-hero-tagline">Parse conda-forge, Spack, and EasyBuild into one canonical manifest and planned SBOM; solve product profiles with Resolvo; emit conventional recipes; build-evaluate them on a declarative target.</p>
     <div class="eb-hero-pills">
       <span>package plan</span>
       <span>package bump</span>
       <span>resolvo SAT</span>
       <span>planned SBOM</span>
       <span>typed campaigns</span>
     </div>
     <p class="eb-hero-flow">recipe → manifest + SBOM → profiles + Resolvo → .eb set → campaign evidence</p>
   </div>

============
``eb-stack``
============

:Author: `Rohit Goswami <https://rgoswami.me>`_

.. important::

   *New here?* → :doc:`tutorial`

   *Whole annual rebuild?* → :doc:`howto/run-annual-bump`

   *New package from conda-forge/Spack?* → :doc:`howto/new-package`

   *Existing recipe bump?* → :doc:`howto/bump-recipe`

   *CLI surface?* → :doc:`reference/cli`

   *Bundle schemas?* → :doc:`reference/package-bundles`

   *Build targets?* → :doc:`reference/targets`

   *Campaign findings?* → :doc:`reference/campaigns`

   *Repair a failed campaign?* → :doc:`howto/repair-campaign`

.. grid:: 1 1 2 2
   :gutter: 2

   .. grid-item-card:: Tutorial — package to verified build
      :link: tutorial
      :link-type: doc

      Plan conda-forge eOn, inspect its lock/recipe, and drive a target campaign.

   .. grid-item-card:: Annual rebuild (operator)
      :link: howto/run-annual-bump
      :link-type: doc

      Generate canonical bump bundles, solve the stack, and repair typed findings.

   .. grid-item-card:: Solve a stack lock
      :link: howto/solve-lock
      :link-type: doc

      Co-select a generation with resolvo; emit lock and build list.

   .. grid-item-card:: Reproduction fidelity
      :link: explanation/fidelity
      :link-type: doc

      Frozen real-recipe regressions, empirical claims, and limitations.

   .. grid-item-card:: Package bundle contract
      :link: reference/package-bundles
      :link-type: doc

      Manifest, planned SBOM, profile locks, residuals, and variant recipes.

   .. grid-item-card:: Targets and campaigns
      :link: reference/targets
      :link-type: doc

      Layered routing, container isolation, persisted findings, and repairs.

.. toctree::
   :maxdepth: 1
   :caption: Getting Started

   tutorial

.. toctree::
   :maxdepth: 1
   :caption: How-To Guides

   howto/run-annual-bump
   howto/new-package
   howto/repair-campaign
   howto/solve-lock
   howto/emit-reports
   howto/bump-recipe
   howto/root-priority

.. toctree::
   :maxdepth: 2
   :caption: Reference

   reference/cli
   reference/package-bundles
   reference/targets
   reference/campaigns
   reference/rust-api
   reference/policy
   reference/formats

.. toctree::
   :maxdepth: 1
   :caption: Explanation

   explanation/lifecycle
   explanation/architecture
   explanation/optimization-model
   explanation/parser-approach
   explanation/fidelity

.. toctree::
   :maxdepth: 1
   :caption: Project

   contributing
   changelog
#+end_export

* What it is

  ~eb-stack~ parses conda-forge, Spack, and [[https://docs.easybuild.io/][EasyBuild]] recipes into one
  canonical package plan. It emits a planned CycloneDX SBOM, materializes
  independently installable profiles, applies distribution/site preferences
  inside [[https://github.com/mamba-org/resolvo][Resolvo]], writes one lock and conventional ~.eb~ file per profile,
  and drives the resulting bundle through a persisted target-routed build
  campaign. Existing-recipe bumps use the same manifest, SBOM, solver, and
  campaign artifacts as new packages.

* What it is not

  - *Not* an EasyBuild replacement. ~eb-stack~ routes ~eb --robot~ through a
    configured target; EasyBuild still implements all build and install steps.
  - *Not* a general-purpose Python dependency resolver. The domain model is
    EasyBuild's ~name~ / ~version~ / ~toolchain~ / ~dependencies~ shape only.
  - *Not* a post-install compliance scanner. The emitted SBOM is a *planned*
    inventory. Binary verification is a separate campaign claim.

* Install

  No public release is published. Build from source with Rust 1.88 or newer:

#+begin_src bash
git clone https://github.com/HaoZeke/eb-stack.git
cd eb-stack
cargo test --locked
cargo build --locked --release
# binary: target/release/eb-stack
install -Dm755 target/release/eb-stack ~/.local/bin/eb-stack
#+end_src

  Verify:

#+begin_src bash
eb-stack --version
#+end_src

* Documentation map

  - [[file:tutorial.org][Tutorial: foreign recipe to verified EasyBuild package]] -- start here.
  - [[file:howto/run-annual-bump.org][How to run the annual toolchain-generation bump]] --
    operator guide for a whole rebuild (what it closes, the residual, the
    claim ladder, and the CI-known bumps).
  - [[file:howto/new-package.org][Author a new package from conda-forge or Spack]] --
    canonical plan + profiles + Resolvo + campaign.
  - How-to guides: [[file:howto/solve-lock.org][solve a stack lock]],
    [[file:howto/repair-campaign.org][repair a failed campaign]],
    [[file:howto/emit-reports.org][emit a build list and stack diff]],
    [[file:howto/bump-recipe.org][bump a recipe]],
    [[file:howto/root-priority.org][declare root priority]].
  - Reference: [[file:reference/cli.org][CLI]],
    [[file:reference/package-bundles.org][package bundles]],
    [[file:reference/targets.org][build targets]],
    [[file:reference/campaigns.org][campaign state and findings]],
    [[file:reference/policy.org][policy JSON schema]], and
    [[file:reference/formats.org][whole-stack output formats]].
  - Explanation: [[file:explanation/architecture.org][architecture]],
    [[file:explanation/optimization-model.org][the optimization model]],
    [[file:explanation/parser-approach.org][the parser approach]],
    [[file:explanation/fidelity.org][reproduction fidelity and limitations]],
    [[file:explanation/lifecycle.org][from recipe to installed stack]].
  - Project: [[file:contributing.org][contributing]], [[file:changelog.org][changelog]].

* Citation

  For unreleased source, cite the repository revision you used. The
  [[https://github.com/HaoZeke/eb-stack/blob/main/CITATION.cff][CITATION.cff]] file provides structured author and project metadata.

#+begin_src text
Rohit Goswami, eb-stack (source revision <commit>), https://github.com/HaoZeke/eb-stack
#+end_src

* License

  MIT. See [[https://github.com/HaoZeke/eb-stack/blob/main/LICENSE][LICENSE]] in the repository.

* Build these docs

#+begin_src bash
cd eb-stack
pixi run -e docs docbld
# HTML -> docs/build/index.html
#+end_src

  Org sources live under ~docs/orgmode/~; Emacs ~ox-rst~ publishes to
  ~docs/source/~; Sphinx + *Shibuya* builds the site.
