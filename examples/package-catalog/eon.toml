schema_version = 1

# When the robot has no readcon-core, close the hole from this EasyBuild recipe.
# Pair with eon.toml [[dependencies.requirements]] name = "readcon-core".
[[packages]]
name = "readcon-core"
provider = "easybuild-bump"
version = "0.13.1"
source = "../packages/companions/r/readcon-core/readcon-core-0.13.1-GCCcore-15.2.0.eb"
toolchain = { name = "GCCcore", version = "15.2.0" }
