schema_version = 1
name = "eon-foss-2026.1"

[toolchain]
name = "foss"
version = "2026.1"

# Core + with_rgpot product: no cross-generation PyTorch/xtb pins.
# Eigen 5 matches the safemath core-guard patch for this generation.
[[pins]]
name = "Eigen"
version_requirement = "==5.0.0"
toolchain = { name = "GCCcore", version = "15.2.0" }
versionsuffix = ""
mode = "preferred"
source = "eOn Eigen 5 safemath path"
