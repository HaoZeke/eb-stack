schema_version = 1
name = "foss-2026.1-defaults"

[toolchain]
name = "foss"
version = "2026.1"

# Add site or distribution constraints as pins. Use mode = "preferred" for a
# fallback-capable preference and mode = "locked" for a mandatory selection.
# Optional toolchain and versionsuffix fields constrain one exact EasyBuild
# artifact. A version-only pin searches the target hierarchy; cross-generation
# selection requires an explicit toolchain.

# Package-specific policies can extend this template without imposing their
# compatibility identities on unrelated plans. See eon-foss-2026.1.toml.
